# thearkplatform.com

Company website for The Ark Platform LLC. Plain HTML and CSS, no build step.

- `public/` is the publish directory. Home, `/privacy/`, `/support/`, and a `404.html`.
- Hosted as a Render static site in The Ark workspace.
- Fonts load from Google Fonts (Public Sans). Everything else is inline.
